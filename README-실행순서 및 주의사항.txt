실행순서
1. 기본적인 환경 구축(Python, eclipse, STS, MySQL...)
2. factory.sql로 DB 만들기
3. DB용 CSV모음 폴더 안의 CSV파일을 테이블에 각각 집어넣기
4. 파이썬_서버_실행.bat 실행
- 여러 라이브러리 필요 ex) folium, googletrans==4.0.0-rc1, Flask
5. factoryPick 실행
- 오류날 시 appliaction.properties의 DB 비밀번호 설정일 확률이 높음, 자기 DB 비밀번호에 맞게 변경


주의
- glove모델이 1기가 이상이므로 git에 안올라감
- CSS 코드가 개판.
